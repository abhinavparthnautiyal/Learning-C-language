#include<stdio.h>
#include<stdlib.h>

struct node
{
    int data;
    struct node *next;
};
struct node *head == NULL;


int main()
{
  printf("Choose from the following actions to be performed on the linked list as per choice:\n");
  printf("Press 1, create a linked list");
  printf("Press 2, Display a linked list ");
}